pub mod parse {

    #[derive(Debug)]
    pub enum Tostsken {
        // Statement(String),
        // Expression(String),
        // Function(String),
        Word(String),
        // Number(String),
        OperatorOrSthIdk(String),
    }
}
